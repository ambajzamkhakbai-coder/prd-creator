# [产品名称] 需求逻辑定义

## 文档信息

| 字段 | 内容 |
|---|---|
| 产品名称 | [产品名称] |
| 文档版本 | v1.0 |
| 创建日期 | [YYYY-MM-DD] |
| 状态 | 已确认 |

---

## 1. MVP 需求范围

### 1.1 纳入MVP的功能

| 编号 | 功能点 | 优先级 | 纳入理由 | 优先级判断理由 |
|---|---|---|---|---|
| F01 | [功能名称] | P0 | [为什么MVP需要这个] | [为什么是P0：例如缺它无法完成核心闭环] |
| F02 | [功能名称] | P0 | [为什么MVP需要这个] | [为什么是P0] |
| F03 | [功能名称] | P1 | [为什么MVP需要这个] | [为什么是P1：例如显著影响留存但非必需] |

> 优先级标准：**P0** = 核心闭环必需；**P1** = 显著体验提升但非必需；**P2** = 锦上添花。

### 1.2 优先级排序总览

- **P0（核心闭环必需）**：F01 [功能名]、F02 [功能名]……
- **P1（显著体验提升）**：F03 [功能名]……
- **P2（锦上添花）**：[功能编号 + 功能名] 或 无

### 1.3 暂不纳入MVP的功能

| 编号 | 功能点 | 暂缓原因 |
|---|---|---|
| F10 | [功能名称] | [为什么MVP阶段不需要] |
| F11 | [功能名称] | [为什么MVP阶段不需要] |

### 1.4 范围边界

- **范围内**：[明确什么在MVP范围内]
- **范围外**：[明确什么不在MVP范围内]

---

## 2. 模块需求逻辑

> 编号约定：功能点编号沿用第 1 节 MVP 范围表的 `F0x` 号，加所属模块前缀（如 `M01-F01` 中的 F01 即范围表的 F01），保证可追溯，不另起编号。

### 2.1 [模块A名称]

#### 模块目标

[一句话说明这个模块要解决什么问题]

#### 核心功能点

| 编号 | 功能点 | 优先级 | 说明 |
|---|---|---|---|
| M01-F01 | [功能名称] | P0 | [功能说明] |
| M01-F02 | [功能名称] | P0 | [功能说明] |

#### 交互逻辑

```
用户操作流程：
1. [步骤1]
2. [步骤2]
3. [步骤3]
```

#### 后端逻辑

**数据流转**：

```mermaid
flowchart LR
    A[状态1] -->|触发条件| B[状态2]
    B -->|触发条件| C[状态3]
```

**业务规则**：

| 规则编号 | 规则描述 | 适用条件 |
|---|---|---|
| R01 | [规则内容] | [什么情况下适用] |

**列表默认排序规则**（仅当模块含列表/筛选/排行榜等场景时填写；否则整段删除，无需保留占位）：

| 场景 | 主排序字段 | 方向 | 平局兜底 | 空值处理 | 位置 | 可切换维度 |
|---|---|---|---|---|---|---|
| [列表名，如"订单列表"] | [如 created_at] | DESC | id DESC | 无 / NULLS LAST | 服务端 / 客户端 | [可选项列表，或"不开放切换"] |

- **索引建议**：[如 `(created_at DESC, id DESC)` 联合索引]
- **分页方案**：[offset 分页 / 游标分页]
- **筛选与排序关系**：[如"切换排序保留筛选；切换筛选保留排序；重置按钮两者一起回到默认"]

#### 边界条件与异常处理

| 场景 | 处理方式 |
|---|---|
| [异常场景1] | [如何处理] |
| [异常场景2] | [如何处理] |

---

### 2.2 [模块B名称]

#### 模块目标

[一句话说明这个模块要解决什么问题]

#### 核心功能点

| 编号 | 功能点 | 优先级 | 说明 |
|---|---|---|---|
| M02-F01 | [功能名称] | P0 | [功能说明] |

#### 交互逻辑

```
用户操作流程：
1. [步骤1]
2. [步骤2]
```

#### 后端逻辑

**数据流转**：

```mermaid
flowchart LR
    A[状态1] -->|触发条件| B[状态2]
```

**业务规则**：

| 规则编号 | 规则描述 | 适用条件 |
|---|---|---|
| R01 | [规则内容] | [什么情况下适用] |

#### 边界条件与异常处理

| 场景 | 处理方式 |
|---|---|
| [异常场景1] | [如何处理] |

---

## 3. 模块间依赖关系

```mermaid
flowchart TD
    M01[模块A] --> M02[模块B]
    M01 --> M03[模块C]
    M02 --> M03
```

| 依赖关系 | 说明 |
|---|---|
| 模块A → 模块B | [依赖说明] |
| 模块A → 模块C | [依赖说明] |
